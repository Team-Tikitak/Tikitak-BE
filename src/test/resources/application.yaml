# ./gradlew clean test
spring:
  datasource:
    url: jdbc:h2:mem:wadadakk;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    driver-class-name: org.h2.Driver
    username: sa
    password:

  flyway:
    enabled: false

  jpa:
    hibernate:
      ddl-auto: create-drop

r2:
  access-key-id: test-access-key
  secret-access-key: test-secret-key
  bucket-name: test-bucket
  endpoint: https://example.com
      
