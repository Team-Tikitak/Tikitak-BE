spring:
  flyway:
    enabled: true
    locations: classpath:db/migration
    validate-on-migrate: true
    postgresql:
      transactional-lock: false

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
